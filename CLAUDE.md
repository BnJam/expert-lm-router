

## Tools

You must use `librarian` and it's associated subcommands that have been decompiled as individual
tooling for filesystem operations such as discovery and modification of files.
The `librarian` tool is run as a commandline tool.
